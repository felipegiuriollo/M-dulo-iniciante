// array, objetos

let meta = {
    value: 'Ler um livro por mês',
    checked: false,
    imprime: (info) => {
        console.log(info)
    }
}

meta.imprime(meta.checked)

// function      // arrow function
const criarMeta = () => {}

function criarMeta () {}